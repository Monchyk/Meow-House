# FRIEND VISIT — prep (2026-09-19)

_The remote collaborator arrives in person. They've built on this remotely for a while;
tomorrow they see it live and **help make the story.** Working mode: **prompt-as-tooling** —
Claude drives the build live while the two of you steer._

## Who / role
- Friend = **story collaborator.** Their core job: shape the **scene story** — which scenes,
  in what order, the narrative arc (the "movie"). Not a spectator.

## What we do together (K., 2026-09-18)
1. **Handpick scenes** → decide which make the 6–8 rotation, add them to the canon player.
2. **Colours / palettes** → live-recolor scenes with a picker.
3. **Sequence into a story** → the friend's lead; storybook is the workspace.

## Have ready (tee-ups, build first thing via prompting — not pre-built tonight)
- **Live-recolor palette picker** (the one tool K. named). Scope = **smallest/most useful:**
  pick a scheme from the 12-atlas → applies to the **running scene instantly.**
  Foundation already exists: `palettes.js` (12-scheme atlas) + every scene already takes a
  palette. So this is a small build, not from scratch. (Custom-palette authoring + per-scene
  memory are possible later; not day-one.)
- **Canon player = `web/player.html`** (director-host; butterfly + baseline live). Adding a
  scene = a register() factory. That's where handpicked scenes land.
- **Storybook workspace:** `scenes/story.md`, `scenes/scenes.md`, `scenes/opinionated.md` —
  where the sequence/narrative gets written as they pick.

## First move when they walk in
Open the **live player** + the **storybook** side by side — they jump straight into making
the story, not watching a demo.

## Out of scope for the visit (parked)
Pendulum Well (dropped), the toy-train clock (post-gig hardware, `docs/TRAIN-CLOCK.md`),
the redrawn-cat art layers.
